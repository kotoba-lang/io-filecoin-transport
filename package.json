{
  "name": "io-filecoin-transport",
  "private": true,
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljk",
    "live": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/live.cljk",
    "probe:provider": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/probe-provider.cljk",
    "probe:delegated": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/probe-delegated.cljk"
  },
  "comment": "@noble/curves remains until eth-crypto cljs path is rewired.",
  "dependencies": {
    "@noble/curves": "^2.2.0"
  },
  "devDependencies": {
    "@filoz/synapse-core": "^0.7.1",
    "viem": "^2.55.10"
  }
}
